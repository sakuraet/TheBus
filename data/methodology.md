# Route Data Cleaning

`trips.txt` gives the parameters `direction_id` in binary (ie. 0 & 1), doesn't say which is inbound vs outbound
- look for `headsign` which notates which direction it's headed in 